from django.urls import path
from . import views

urlpatterns = [
    # path('nfts/<str:token_id>/', views.nft_detail, name='nft_detail'),
    # path('properties/<int:property_id>/nfts/', views.property_nfts, name='property_nfts'),
]
